name: synheart_wear
description: Unified wearable SDK for Synheart (HR, HRV, steps, energy, stress).
version: 0.1.0
homepage: https://github.com/synheart-ai/synheart_wear
environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'
dependencies:
  flutter:
    sdk: flutter
  collection: ^1.18.0
  uuid: ^4.4.0
  path_provider: ^2.1.4
  health: ^13.2.1
  platform: ^3.1.6
  encrypt: ^5.0.1
  # Platform bridges (add as needed)
  # health: ^9.0.0
  # google_fit: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^6.0.0
flutter:
  # This is now a pure Dart package that uses the health plugin
  # No platform-specific code needed as we delegate to health package
