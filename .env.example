# Synheart Wear - Environment Variables Example
# 
# Copy this file to .env.local for local development:
#   cp .env.example .env.local
#
# For production, use service-specific .env files or set environment variables directly.

# ============================================================================
# WHOOP API Credentials
# ============================================================================
# Get these from: https://developer.whoop.com/
WHOOP_CLIENT_ID=your_whoop_client_id_here
WHOOP_CLIENT_SECRET=your_whoop_client_secret_here
WHOOP_WEBHOOK_SECRET=your_whoop_webhook_secret_here
WHOOP_REDIRECT_URI=http://localhost:8000/v1/oauth/callback

# ============================================================================
# Garmin API Credentials
# ============================================================================
# Get these from: https://developer.garmin.com/
GARMIN_CLIENT_ID=your_garmin_client_id_here
GARMIN_CLIENT_SECRET=your_garmin_client_secret_here
GARMIN_WEBHOOK_SECRET=your_garmin_webhook_secret_here
GARMIN_REDIRECT_URI=http://localhost:8001/v1/oauth/callback

# ============================================================================
# AWS Configuration (Production Only)
# ============================================================================
# These are only used in production (api.py), not in local dev (api_local.py)
# Local dev uses mocked AWS services (file-based storage)
DYNAMODB_TABLE=cloud_connector_tokens
KMS_KEY_ID=arn:aws:kms:us-east-1:123456789012:key/your-kms-key-id
SQS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/123456789012/whoop-events
WHOOP_SQS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/123456789012/whoop-events
GARMIN_SQS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/123456789012/garmin-events

# ============================================================================
# Local Development Mode
# ============================================================================
# Set to true to use mocked AWS services (file-based token storage)
# This is automatically set when using api_local.py
LOCAL_MODE=true
