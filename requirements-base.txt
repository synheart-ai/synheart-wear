# Base requirements shared across all services
# This file contains common dependencies used by all FastAPI services
# 
# Usage in services:
#   pip install -r requirements-base.txt
#   Or include in service requirements.txt with: -r ../../requirements-base.txt

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Data validation and parsing
pydantic>=2.0.0

# HTTP client
httpx>=0.27.0

# AWS SDK
boto3>=1.34.0

# Utilities
python-dateutil>=2.8.0
python-dotenv>=1.0.0

# Local dependencies (editable installs)
-e libs/py-cloud-connector
-e libs/py-normalize

